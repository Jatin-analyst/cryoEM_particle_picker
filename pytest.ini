[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
markers =
    property_test: Property-based tests using Hypothesis
    unit_test: Unit tests for specific functionality
    integration_test: Integration tests across components
